language: ruby
rvm:
  - 2.2.1
  - ruby-head
  - rbx-2
  - jruby-head
bundler_args: --without development --deployment
cache: bundler
script: bundle exec rspec --color spec --format progress
branches:
  only:
    - master
    - develop
